[theme]
base = "dark"

# Input/widget background colour
secondaryBackgroundColor = "#2a2a2a"

# Text colour inside inputs and labels
textColor = "#ffffff"

# Primary accent (buttons, sliders, etc.)
primaryColor = "#2a2a2a"

# Main page background
backgroundColor = "#0E0E0E"

# Font
font = "sans serif"
